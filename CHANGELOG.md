# Changelog

This file is maintained **by hand** — record notable changes in the
`[Unreleased]` section as part of the PR that makes them. Format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

Per-version release notes for tagged releases are published on the
[GitHub Releases page](https://github.com/wyre-technology/conduit/releases) —
`semantic-release` generates them from commit history at release time. It no
longer commits version sections back into this file (that required a direct
push to `main`, which branch protection correctly rejects); the released-version
detail lives on Releases, the human narrative lives here.

The version history below (1.11.0 and earlier) was generated by
`@semantic-release/changelog` before the switch and is kept as the record.

## [Unreleased]

### Changed

- **Brand defaults rebranded to "Conduit" / "AI Gateway for MSPs"** (was
  "Wyre Technology" / "Customer MCP Gateway"). 2026-06-09 SEO/AEO/GEO audit
  flagged that the staging-app title diluted the product entity that answer
  engines (Claude/ChatGPT/Perplexity) need to learn. Resellers continue to
  white-label via `BRAND_NAME` / `BRAND_TAGLINE` / `BRAND_LOGO_URL` env vars;
  the PROVIDER entity (WYRE Technology) is asserted in the landing-page
  Organization JSON-LD regardless of reseller brand.

### Added

- **SEO/AEO/GEO metadata block on the non-customer landing page.** Emits
  `<link rel="canonical">`, Open Graph (`og:title`, `og:description`,
  `og:image`, `og:type`, `og:url`, `og:site_name`), Twitter Card
  (`twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`),
  and three JSON-LD blocks: Organization (WYRE Technology + sameAs LinkedIn /
  GitHub / Facebook / wyretechnology.com / mcp.wyre.ai / conduit.wyre.ai),
  WebPage with nested SoftwareApplication ("Conduit"), and FAQPage with
  three answer-engine-optimized questions (What is Conduit / Who is it for /
  How it relates to the WYRE MCP Gateway). Schema shape matches the Astro
  marketing site at `conduit.wyre.ai` so entity identity is preserved across
  the Astro→Fastify cutover. New `BRAND_OG_IMAGE` env var overrides the
  Open Graph image; defaults to `{BASE_URL}/assets/og-conduit.png` for the
  forthcoming Conduit OG card. Customer-white-labeled landings keep their
  own entity surface and do NOT inherit Conduit's JSON-LD.

### Fixed

- **Reseller dev-language sweep RC5 — customer-visible dev-language removed
  from /org/customers + customer-detail Overview/Billing/Settings.** Friday
  PR #360 closed the seam()-helper leaks in `reseller-customer-tabs.ts` but
  did not cascade to the listing or detail-page bottoms. This sweep removes:
  (a) `reseller-customers.ts` — the bottom "currently renders mock data; the
  data source has shipped, and a follow-up PR wires this page-handler"
  paragraph + the disabled Impersonate/More-actions buttons with
  `lands in a follow-up` tooltips + the disabled status-filter `Status
filtering lands with the Track A customer-status field` tooltip;
  (b) `reseller-customer-detail.ts` — the bottom "Analytics on this page
  are live — … render once the reseller-scoped … endpoints land" paragraph
  - the disabled `Impersonate user` button with
    `User impersonation is its own auth surface — lands in a follow-up`
    tooltip; (c) `reseller-customer-tabs.ts` — the Billing-tab
    `Live customer billing data lands when the reseller customer-billing
endpoint ships` paragraph (replaced with a neutral "No billing data yet
    for this customer"), the Settings-tab Plan-tier field (removed; BUSINESS
    legacy plan UI removed entirely consistent with PR #360 listing removal),
    the Settings-tab `Fields are read-only until the Track A reseller-
settings endpoint lands` paragraph, and the `Customer suspension lands
with the Track A provisioning endpoint` + `Settings persistence lands
with the Track A reseller-settings endpoint` button tooltips. Discovered
    via chrome-devtools MCP design sweep of all 26 reseller-side surfaces on
    staging.conduit.wyre.ai. Tests updated to assert the new shape — 37
    tests across the 3 files all green.

### Added

- **Microsoft Copilot Studio connect guide — service-client (M2M) path documented (Aaron 2026-06-02).**
  Copilot Studio reaches Conduit through a Power Platform custom connector, and
  Power Platform's "Generic OAuth 2.0" doesn't send PKCE — so the standard MCP
  OAuth 2.1 sign-in path does not work for it. New guide
  `docs/guides/connecting-copilot-studio.mdx` walks the supported route end to
  end: minting a Conduit service client (Business plan), building the Power
  Apps custom connector with `x-ms-agentic-protocol: mcp-streamable-1.0`,
  authenticating with `Authorization: Bearer <token>` (API Key auth type),
  wiring the connector into a Copilot Studio agent, and handling the
  per-end-user consent on first invocation. Also documents Copilot Studio's
  hard cap of **70 tools per MCP server per agent** (mitigation: per-vendor
  connectors against `/v1/<vendor>/mcp`) and the 1-hour bearer-token rotation
  requirement that comes with the API Key path. Verified end-to-end against
  `mcp.wyre.ai` on 2026-06-02 — agent invoked `datto-rmm__datto_list_sites`
  through the gateway and rendered a live Markdown table of real Datto RMM
  sites. Touches: `docs/src/content/docs/guides/connecting-copilot-studio.mdx`
  (new), `docs/src/content/docs/reference/supported-clients.mdx` (matrix +
  rewritten "Clients that don't speak MCP natively" section so Copilot Studio
  is no longer listed as a roadmap item), `docs/astro.config.mjs` (sidebar),
  `docs/src/lib/faq.ts` + `docs/src/lib/faq.test.ts` (FAQ_DATA entries +
  MDX_FOR_SLUG mapping so the FaqSchema component and the byte-faithful
  drift-test cover the new page).

- **Free-org cutover-policy — 14-day decide-or-revert grace via gate lazy-flip (Aaron 2026-05-29).**
  Companion to the flat-pricing rip-out (#275). Existing mcpgw 'free'-plan orgs
  receive a 14-day window post-cutover to convert to paid; non-deciders are
  service-denied at T+14d. Implementation:
  - **`scripts/migrate-from-mcp-gateway.ts`** — new `seedFreeOrgGraceSubscriptions`
    table-step seeds a LOCAL subscriptions row (NOT a Stripe subscription) for
    each mcpgw 'free' org: `status='trialing'`, `cancel_at_period_end=TRUE`,
    `current_period_end = NOW() + 14 days`, placeholder `stripe_subscription_id`
    of `grace:<orgId>` (collision-free vs Stripe's `sub_*` id space). Seeded
    AFTER `migrateSubscriptions` so the grace row's `created_at` is newer than
    any real Stripe sub copied over — `getSubscription`'s
    `ORDER BY created_at DESC LIMIT 1` makes the grace row authoritative until
    a real Checkout supersedes via `subscription.updated` UPSERT.
  - **`src/billing/gate.ts isServiceActive`** — extended with the
    cutover-grace / cancel-at-period-end branch. When a row's status is
    `active` or `trialing` AND `cancel_at_period_end=TRUE` AND
    `current_period_end ≤ now`, service is denied. Evaluation is read-time —
    the T+14d flip happens by-time-elapsed; **no cron, no scheduled write**.
    The same branch also gives Stripe scheduled-cancel parity (a paid sub the
    owner has chosen to cancel-at-period-end ends service correctly at period
    end without waiting on a webhook).
  - **`SubscriptionLike` interface** — extended with optional `current_period_end`
    and `cancel_at_period_end` fields; existing callers that only pass
    `(status, first_failure_at)` keep working unchanged. `OrgService.getSubscription`
    SELECTs the two new columns.
  - **No data is deleted. No Stripe trial extended. No Stripe customer minted.**
    The grace row is a local marker; a real Stripe subscription created via
    Checkout in the 14-day window supersedes by `created_at DESC` authority.
  - **Test coverage (gate.test.ts +9 cases)** — asymmetric-pair shape per ruby's
    rot-vector-closure pin: flagged + future → ACTIVE; flagged + past → DENIED;
    unflagged + past → ACTIVE (flag-off axis); status-axis pair (trialing +
    active both DENIED on flag+past); boundary inclusive on the deny side;
    ISO-string `current_period_end` accepted; back-compat preserved for rows
    without the new fields.

### Changed

- **FLAT PRICING — one plan, no tiers, no credits, no call-gating (Aaron 2026-05-26).**
  Conduit moves to a single everything-included plan: **$399/org base + $39 per
  billable seat** (`ORG_FEE_CENTS=39900`, `PER_SEAT_PRICE_CENTS=3900`). The
  former free / pro / business tiers, the customer credit-overage model, and the
  per-tier call-gating are removed. Specifics:
  - **One plan slug, `conduit`.** `plan-catalog.ts` collapses to a single
    everything-included plan (all features on, all limits unlimited). `getPlan`
    resolves ANY non-empty slug — including a legacy `free`/`pro`/`business`
    value on an un-migrated row — to the one plan. `isPaidPlan` is therefore true
    for any resolvable slug; service-delivery is decided by the dunning-aware
    `isServiceActive` (subscription status + grace window), not the plan tier.
  - **Seat inclusion kept (Shape-A).** The first two agent seats are included;
    agent #3+ bills; humans always bill. `billableSeats = humans + max(0,
agents − 2)`; `monthlyTotalCents = 39900 + 3900 × billableSeats`.
  - **Rate limit divorced from the plan.** A flat anti-abuse ceiling
    (`ANTI_ABUSE_RATE_PER_HOUR = 5000`) lives as a module constant in
    `prices.ts`, not on the plan object, so it can never drift back into a
    pricing tier. The per-user(-per-vendor) Fastify rate-limit mechanism is
    unchanged — only the max is flattened.
  - **Customer credits removed; usage-log substrate kept.** The credit-pack
    purchase route, the credit-block balance/deduct/grant methods, the admin
    comp-credit panel, and the webhook `addBlock` handler are gone;
    `credit_blocks` is dropped (migration 037). `credit_ledger` (the per-call
    usage LOG read by reseller-channel wholesale invoicing + the admin
    usage-burn view) is KEPT — customer pricing does not touch reseller-channel
    billing.
  - **Cancellation flows through subscription status (Shape-A′).** Since
    `isPaidPlan` is now always true, service-denial flows entirely through
    `subscriptions.status`. New orgs seed a `trialing` subscriptions row at
    provisioning; `customer.subscription.updated` mirrors Stripe's status on
    every transition (non-terminal: status only; terminal-cancel: status +
    clears the dunning clock); `customer.subscription.deleted` upserts
    `status='canceled'` and clears the clock. The plan never flips.
  - **`Organization.plan` is typed `string`** (was the narrowed `'conduit'`):
    the column is a free-form field that still carries legacy slugs on
    un-migrated rows during the migration window — narrowing it would be
    untruthful about what the column can hold.
  - **Migration 037** (`037_flat_pricing.sql`, idempotent + non-destructive):
    `UPDATE organizations SET plan='conduit'` (legacy values collapse uniformly;
    real free orgs are grandfathered-by-construction via the no-sub defensive
    branch of `isServiceActive`) + `DROP TABLE IF EXISTS credit_blocks`.

  > **Flagged for the merge owner (NOT decided in this PR):**
  >
  > 1. **Existing free-org transition policy** — collapsing `plan` to `conduit`
  >    is service-safe (grandfathered by construction), but whether/when/how to
  >    _bill_ the ~11 real free orgs live in `mcpgw-prod` is an account-management
  >    decision for Aaron, not this code change.
  > 2. **Cutover migration sequencing** — migration 037 must run AFTER
  >    `scripts/migrate-from-mcp-gateway.ts`, OR that org-migration must map
  >    `plan → conduit` itself (option-(b) is order-independent-by-construction).
  >    `conduit-prod` is empty pre-cutover; the 11 real free orgs live in
  >    `mcpgw-prod`. Deploy-sequencing owned by dev/forge in the cutover runbook.

### Added

- **`docker-build-check` — PR-time gate for the full Docker build context.**
  The `docker` job runs only on push-to-main, so a Dockerfile build-context
  break (a stage importing a path outside its `COPY` context) shipped green and
  reddened main post-merge — #249's docs-builder stage could not resolve
  `../../../src/faq` because it copies `docs/` only, while the required
  `docs-build` check builds in full-repo context and resolved it (false
  assurance). This new PR-triggered job builds the real image (`push:false`) so
  a build-context break fails _before_ merge. Added to branch-protection
  required checks. Closes the gap class that shipped #249 (fixed-forward by #250).

- **FAQPage structured data on the FAQ docs (getting-started + connecting-a-client).**
  schema.org `FAQPage` JSON-LD injected on the two pages that carry a visible
  `## Frequently asked questions` section (6 + 4 Q/A), for rich-results / AEO.
  FAQPage is a derive-from-visible (mechanical) channel — Google rejects schema
  whose answer text does not match the rendered answer — so the schema mirrors
  the visible Q/A: `acceptedAnswer.text` is the markdown-normalized
  (`**bold**`/`` `code` ``/`[text](url)` → display text) rendering. Two
  enforced invariants (`src/faq.test.ts`): (1) a **schema-matches-visible**
  drift-catch — `extractFaq(<page>.mdx)` must deep-equal `FAQ_DATA` for both
  question AND answer (raw==raw), so editing a visible Q/A without the schema
  fails CI; (2) a **`</script>` breakout** defense — answer text is injected
  with `<` escaped to `<` (case-insensitive by construction, vs a naive
  lowercase `</script>` replace), proven by a case-variant `</ScRiPt>` +
  round-trip-`JSON.parse` test. `src/faq.ts` is the pure module (data + builder
  - extractor); `docs/src/components/FaqSchema.astro` emits the per-slug tag.

- **Google Analytics (GA4) on the docs, prod-gated client-side.** The standard
  `gtag.js` snippet (measurement ID `G-V3W6M1YEHL`) is injected into every docs
  page `<head>` via Starlight `head` — but wrapped in a hostname guard that
  injects the `gtag.js` `<script>` and runs init ONLY when
  `location.hostname === 'conduit.wyre.ai'`. The docs are built once into the
  gateway image and served by both the staging and prod gateways
  (single-image-both-envs), so a baked tag can't be build-time-gated; the
  client-side guard is the runtime equivalent of the `computeDocsNoindex`
  prod-apex-host discriminator. Off-prod (staging / preview / local) it makes
  **zero** request to `googletagmanager.com` and fires nothing — keeping the GA
  property clean of pre-launch/internal traffic. The measurement ID is a public
  client-side identifier (ships in HTML by design, not a secret).

- **Technical-SEO/GEO infra for the docs (sitemap + JSON-LD + llms.txt route).**
  Pre-cutover SEO/AEO/GEO groundwork on the same gateway-static-serve surface as
  the docs + robots policy. (1) `@astrojs/sitemap` emits
  `/docs/sitemap-index.xml` — the exact URL the prod `robots.txt` already
  advertised (closing that advertised-but-404 reference) — with a `filter` that
  excludes `internal/`, so the per-agent system-prompt docs are not discoverable
  via the sitemap channel any more than via the page-serve, robots, or llms.txt
  channels (Finding A: exclude across all crawler-discovery channels). (2) Global
  schema.org JSON-LD (`Organization` + `SoftwareApplication`) injected into every
  docs page `<head>` via Starlight `head`. Claims are grounded + minimal
  (name/category/description/url/publisher) — no `aggregateRating`/`offers`:
  engines ingest structured data as fact, so unbacked claims are a trust breach.
  (3) A `/llms.txt` route (llmstxt.org GEO convention), env-gated by the SAME
  `computeDocsNoindex` discriminator as `robots.txt`: served only on the indexed
  prod surface, 404 on a noindex surface (a noindex staging surface must not
  advertise an AI-crawler map to pre-launch docs). The body (`src/llms.ts`,
  `LLMS_TXT`) is the curated docs-content artifact, committed + served
  byte-for-byte; every link target is verified to resolve to a built page before
  shipping (advertised-resource-must-exist), and no `internal/` page appears.
  Tests: `src/llms.test.ts`. Per-page `FAQPage` schema is a content-owned
  follow-up.
- **Docs served at `/docs` without a trailing slash + env-gated robots policy.**
  The Astro/Starlight docs are built, bundled (`Dockerfile` docs-builder stage
  → `public/docs`), and served by `@fastify/static` — and were already live at
  `staging.conduit.wyre.ai/docs/`. But `@fastify/static` lacked `redirect: true`,
  so the no-trailing-slash form a customer types — `/docs`, `/docs/getting-started`
  — 404'd while `/docs/` served 200. Added `redirect: true` (valid with
  `wildcard:false` because the app does not set `ignoreTrailingSlash`); now every
  directory-no-slash 301s to its slashed form. File requests (`/docs/_astro/*`)
  are unaffected — only directories redirect.
  Alongside, a 3-layer-minus-1 env-gated robots policy so non-production
  surfaces are not indexed/AI-ingested: (1) an `X-Robots-Tag: noindex, nofollow`
  response header and (2) a `/robots.txt` that disallows all crawlers + names
  the AI-crawler UAs (GPTBot, ClaudeBot, Claude-Web, anthropic-ai,
  Google-Extended, CCBot, PerplexityBot, Bytespider, Amazonbot, cohere-ai).
  The gate (`src/robots.ts` `computeDocsNoindex`) is **fail-safe**: indexes ONLY
  when `BASE_URL` host is `conduit.wyre.ai`; everything else (staging, the
  pre-cutover kinddesert FQDN, localhost) noindexes — and the index-flip rides
  the RECORD-2 cutover automatically (when `BASE_URL` becomes `conduit.wyre.ai`),
  the same cutover-discriminator as the OAuth-issuer rewrite. `DOCS_NOINDEX=true|false`
  is an explicit override. The build-time Astro `<meta robots>` (layer 3) is
  intentionally NOT used: the single image serves both staging + prod, so a
  baked-in meta tag cannot be runtime-env-gated and would wrongly noindex prod;
  the runtime `X-Robots-Tag` header is the env-gated equivalent and strictly
  more reliable. `noindex` is crawler/AI politeness, not access-control — the
  docs contain no secrets, so politeness-not-authgate is the correct tool.
  Regression tests: `src/static-docs-redirect.test.ts` (directory→301 +
  file→200-not-redirected invariants) and `src/robots.test.ts` (gate's
  prod/staging/kinddesert/localhost/override branches + both robots.txt modes).
  KNOWN minor (separate, docs-content lane): `/docs/favicon.svg` 404s — the
  Starlight HTML references a favicon the build does not emit; cosmetic
  browser-tab-icon, not a delivery-wiring item.
- **`/docs/internal/*` always-noindex belt (Finding A).** A path-matched
  `X-Robots-Tag: noindex, nofollow` header on the internal docs subtree
  REGARDLESS of env — including the indexed prod apex. The `internal/` docs
  carry full per-agent system-prompt contents and must never be indexed.
  Header-based, NOT a `robots.txt Disallow:` (a Disallow on the indexed prod
  robots.txt would advertise the path — a reconnaissance leak). This is
  defense-in-depth: the durable fix is build-excluding `internal/` from the
  published Starlight site (owned by docs-content + enforced by a new
  `/docs/internal/-404` BLOCKING check on the RECORD-2 cutover checklist in
  `azure/CONDUIT-PROD-DEPLOY.md`); this belt covers compliant crawlers in the
  window before that lands and a regression that re-includes `internal/`. Pure
  matcher `isInternalDocsPath` in `src/robots.ts` with branch + sibling-prefix
  (`/docs/internal-changelog`) coverage in `src/robots.test.ts`. The matcher
  decides on the path `@fastify/send` actually serves — it applies the same
  normalization send does (guarded `decodeURIComponent` + `path.posix.normalize`,
  which collapses `//` and resolves `..`) before the prefix test — so encoded
  / doubled / dot-segment variants (`/docs/%69nternal/`, `/docs//internal/`,
  `/docs/foo/../internal/`) that send would serve don't dodge the header.
- **Gateway-side on-prem relay client wired at boot + 3-state classifier.**
  PR #211 added `RelayControlPlaneClient` and the relay-side
  `requireControlPlaneSecret()` refuse-to-boot, but never wired the client
  into the gateway boot path — `src/index.ts` called `unifiedProxyRoutes`
  without a `relayControlPlane` dep, so `CONTROL_PLANE_SECRET` on the
  gateway side was read by **nothing at runtime**. PR #214 ships the relay
  container; PR #217 wired `CONTROL_PLANE_SECRET` into `gateway-app.bicep`;
  this PR connects the wire. Gateway boot now reads both
  `CONTROL_PLANE_RELAY_URL` and `CONTROL_PLANE_SECRET` and dispatches by
  three-state classifier (`classifyControlPlaneBoot()` in
  `src/proxy/relay-control-plane-client.ts`):
  - `wired` (both set) → construct `RelayControlPlaneClient` + pass to
    `unifiedProxyRoutes`; on-prem-routed `/v1/mcp` calls flow through the
    relay.
  - `unconfigured` (both absent) → soft-skip; gateway runs without the
    on-prem path (dev/test friendly + an intentional state for deployments
    that do not yet need on-prem routing).
  - `ambiguous` (exactly ONE set) → refuse-to-boot LOUD with
    named-actionable-choice ("set BOTH or set NEITHER"). Asymmetric
    misconfig is far more dangerous than either fully-on or fully-off
    because it looks "almost configured" while being non-functional.
    Adds `requireControlPlaneSecret()` (parity with
    `src/relay/control-plane-server.ts:331`) as the gateway-side helper.
    Closes warden's #217 structural follow-up + the on-prem axis of
    launch-readiness: the gateway-relay path moves from
    deployable-but-not-functional to deployable-AND-functional. Cross-links
    to PR #211 (client class), PR #214 (relay container), PR #217
    (bicep wiring). Caught via the "defined-but-not-called silent-failure
    class" discipline (N=3 corroboration per boss msg 1779475710507).
- **Layer 1 §8 — customer-facing per-seat billing surface (PR-A).** The
  customer-facing half of the Layer 1 billing model ($600/org base +
  $20/billable-seat; the first 2 agent seats included in the base).
  Surfaces read from the canonical `SeatBilling` snapshot exported by
  `src/billing/seat-service.ts` (the data-layer SoT landed in PR #221) —
  `Object.freeze`d, monthly total pre-derived as `monthlyTotalCents` so no
  template recomputes the charge. `/org/billing` renders the composed bill
  ("$600 base + N seats × $20 = $X/mo"), the inclusion-explicit seat line
  ("9 seats — 5 members + 4 agents (2 included, 2 billed)"), a 14-day-trial
  banner with the bill framed as post-trial (charge amount read directly off
  `monthlyTotalCents`, same number the plan card renders + Stripe will
  charge), and a two-line-item invoice reconcile note (the real invoice
  renders in the Stripe portal — no on-page renderer that could disagree
  with Stripe). The service-client create form shows the agent-seat cost
  ($0 for agent #1/#2, $20/mo for #3+); the members / invitations / domains
  pages surface the $20/mo member-seat cost, including a heads-up that
  domain auto-join adds billable seats silently. Price strings come from
  the named SoT constants in `src/billing/prices.ts` (`BASE_PRICE_CENTS` /
  `PER_SEAT_PRICE_CENTS`), never from per-call view-object fields, so the
  price source-of-truth lives in exactly one place. Route handlers call
  `computeSeatBilling({ humans, agents })` — the pure no-I/O variant
  dev exposed for callers that already hold the counts — to derive the
  snapshot, sidestepping the reserved-tx hang class entirely (a producer-
  side fix to `DefaultSeatService.getSeatCounts` shipped in parallel as
  dev #228). Test fixtures route through a single `makeSeatBilling(humans,
agents)` helper (`src/web/templates/test-helpers/seat-billing-fixture.ts`)
  built on `computeSeatBilling`, so no test builds the `SeatBilling` shape
  by hand. The legacy `src/billing/seat-billing.ts` (mock `SeatBilling` +
  `mockSeatBilling`) is removed — the canonical type from `seat-service.ts`
  is now the only `SeatBilling`. The obsolete disabled "Change plan" button
  and dead invoice-table / payment-method CSS are removed.
- **Reseller onboard-MCP wizard (Surface 3) copy refreshed + wiring-pattern
  copy lifted to a single source (`WIRING_PATTERN_COPY`).** Scribe-drafted
  Surface 3 + 3a copy lands against the onboard-MCP wizard
  (`src/web/templates/reseller-onboard-mcp.ts`): Step 2 sub now talks about
  "per-user credentials (BYOC)" vs "shared credentials" instead of the
  internal "OEM" shorthand; Step 3 table header switches from `Seat` to
  `Role` (the column shows roles, not seat status); Step 4 disabled-CTA
  tooltip switches from the engineer-voice "Onboarding persistence lands
  with the Track A endpoint" to user-voice "Coming with launch."; footer
  note loses the wizard-internals voice while keeping the two load-bearing
  facts (BYOC-only at v1, audit + customer-email gating). The three
  wiring-pattern cards (BYOC / Shared / Self-hosted) gain a permanent
  exported copy const `WIRING_PATTERN_COPY: Record<WiringPatternId, …>`;
  the mock data builder in `routes.ts` now spreads
  `WIRING_PATTERN_COPY[id]` instead of inlining the prose. Track A
  swap-in: the vendor catalog or pattern registry supplies
  `{ id, supported, recommended? }` per pattern, and the same spread
  fills the rest — so copy lives in exactly one place and survives the
  data-source swap. Pairs with scribe's onboarding-docs lane (Surface 1
  #226, Surface 2 #223); closes the copy half of Surface 3 + 3a.
- **Cutover-day validation expanded to three named checks in
  `CONDUIT-PROD-DEPLOY.md`.** Step 5 of the `conduit.wyre.ai` cutover
  sequence now requires `/health` 200 (existing) PLUS two new explicit
  checks: (1) `/.well-known/oauth-authorization-server` `.issuer` =
  `https://conduit.wyre.ai` — proves the bicep ternary
  (`empty(customDomain) ? FQDN : customDomain`) refreshed `BASE_URL` from
  the kinddesert default FQDN; an issuer-mismatch at this point means
  the redeploy did not pick up the new customDomain and OAuth clients
  will reject tokens; and (2) `/v1/mcp` POST returns `401` (auth-gated,
  not 5xx) — proves the MCP path is reachable + auth-rejecting on the
  new env. Converts a standing-implied check (analyst tonight: "verify
  the issuer string post-cutover") into a tracked-checklist-item
  future cutover-day operators inherit as a named step, not
  learned-by-cutover-day-discovery.
- **`CONTROL_PLANE_SECRET` wired into `gateway-app.bicep`.** The 2026-05-22
  bicep-completeness pre-cutover audit found a gap: PR #211 introduced
  `relay-control-plane-client.ts` (`process.env.CONTROL_PLANE_SECRET ?? null`)
  but no bicep wiring landed — both live gateways (`conduit-prod-gateway` +
  `mcpgw-staging-gateway`) had no `CONTROL_PLANE_SECRET` env, so relay
  control-plane calls would silently fail (null === null skip-path) at runtime.
  `gateway-app.bicep` now declares a KV-backed `control-plane-secret` secret
  (resolved by the gateway's system identity from `${keyVaultUri}secrets/
control-plane-secret`) and wires `CONTROL_PLANE_SECRET` as a `secretRef` env
  var. The KV secret itself was provisioned 2026-05-22 into both
  `conduit-prod-kv` and `mcpgw-staging-kv` (symmetric — same value on both
  ends because the relay signs requests with the SAME HMAC secret the gateway
  verifies). `azure/CONDUIT-PROD-DEPLOY.md` gains a "Cutover-day env-refresh"
  subsection cross-linking the requirement so the cutover redeploy never
  rediscovers it. Caught pre-cutover, not at cutover.
- **conduit-prod vendor fleet — `azure/vendor-fleet.bicep` (option-a Piece 1).**
  Provisions the conduit-prod vendor MCP sidecar fleet — one internal-ingress
  Container App `<slug>-mcp` per vendor — into the existing `conduit-prod-env`
  managed environment. A separate template from `main.bicep`: the gateway
  template still does not own the fleet; this one owns the conduit-prod fleet,
  so a gateway redeploy stays lean. The fleet is the **33 internal-sidecar
  vendors** of `vendor-config.ts` (the entries whose `containerUrl` is
  `http://<slug>-mcp:8080`; the 8 external-hosted vendors need no container).
  `azure/vendor-fleet.conduit-prod.bicepparam` carries the 33-vendor list,
  each image **digest-pinned** (resolved from `ghcr.io/wyre-technology/
<slug>-mcp:latest`). Naming the apps `<slug>-mcp` lets the gateway resolve
  every vendor via the `vendor-config.ts` default `containerUrl` with no
  per-vendor `VENDOR_URL_*` wiring. `azure/vendor-fleet-completeness-check.sh`
  is the acceptance gate — it parses `vendor-config.ts` for the sidecar set
  and asserts each `<slug>-mcp` app exists healthy in `conduit-prod-env`, so a
  missing vendor is caught at deploy time rather than as a customer-facing
  "Tool discovery failed: fetch failed" on the Tool Access page.
- **`conduit-prod` deploy path in `deploy.yml` (Phase B).** `deploy.yml` gains
  a third `workflow_dispatch` target — `conduit-prod` — that deploys the
  dedicated own-RG conduit-prod stack (`rg-conduit-prod`, World-A separation).
  Unlike staging/production it keeps all secrets (the 4 deploy params +
  `rootlyWebhookUrl`) in its own `conduit-prod-kv` and fetches them at deploy
  time (the deploy SP holds `Key Vault Secrets User` there) — no
  GitHub-environment secrets to duplicate. The step also runs an idempotent
  `az postgres flexible-server parameter set azure.extensions=PG_TRGM` before
  the deploy, so a fresh conduit Postgres never crashloops `migrate.ts` on the
  unallow-listed `pg_trgm` extension (migration 012). The `Read existing
gateway state` step is env-resolved for conduit-prod's `rg-conduit-prod` /
  `conduit-prod-gateway` naming. `azure/CONDUIT-PROD-DEPLOY.md` documents the
  `pg_trgm` allow-list and the gateway-identity → Key Vault grant as the two
  fresh-stack standup hardening steps.
- **Track A — Audit Log tab wired to real data.** The reseller console's
  per-customer Audit Log tab (`/org/customers/:id/audit`) now serves a live
  reseller-scoped feed of the customer org's MCP tool invocations instead of
  mock rows. New endpoint `GET /admin/reseller/:resellerId/customers/
:customerId/audit` (in `src/reseller/routes.ts`), gated by
  `requireResellerOrCustomerAccess` — the same `:id`-ownership gate the live
  Usage tab uses, with `request_log` RLS underneath (enforced twice). The web
  page additionally swaps the bare `requireResellerAccess` for an
  `:id`-ownership check before rendering any real customer identity, so a
  reseller cannot open `/audit` for a customer it does not own. The tab is a
  live client-fetch shell (`createElement`/`textContent`, no `innerHTML`).
  First of the six mock-first per-org tabs to go live; v1 surfaces tool
  invocations (admin-event entries are a documented follow-up).
- **New-user signup Slack notifications.** When a user creates their first
  membership in any organization (owner or joining an existing org), the sales
  notifier posts to `#conduit-sales` with their details. Uses existing webhook
  infrastructure; fire-and-forget from membership creation points.
- **Two Microsoft MCP vendors mirrored from the WYRE MCP gateway for parity.**
  `microsoft-graph` (Microsoft Graph Enterprise) — a hosted first-party,
  preview-tagged OAuth vendor at `mcp.svc.cloud.microsoft/enterprise`, with no
  sidecar. `azure-mcp` (Azure MCP Server) — a sidecar vendor authenticated by an
  Entra service principal (tenant ID / client ID / client secret), added to
  `docker-compose.yml` with `VENDOR_URL_AZURE_MCP` wired into the gateway. A new
  optional `preview?: boolean` field on `VendorConfig` surfaces a "Preview"
  badge without affecting proxy behavior.
- **Track C — reseller-console / subtenant UI/UX.** The customer-facing
  surfaces a reseller uses to administer its customer orgs, behind the new
  `requireResellerAccess` gate (`requireTeamAccess` + `org.type === 'reseller'`):
  - **Reseller-console shell** — `customer-detail` / `reseller-settings` nav
    modes and the reseller-console nav (Customers, Hierarchy).
  - **Surface 1 — Customers** (`/org/customers`): the customer-org list with
    search + plan filter.
  - **Surface 2 — Customer Detail** (`/org/customers/:id`): per-customer
    overview; analytics wired live to the reseller-scoped customer-dashboard
    endpoints, the rest mock-data-first.
  - **Surface 3 — Onboard MCP wizard** (`/org/customers/:id/onboard-mcp`):
    4-step wizard to onboard an MCP integration for a customer.
  - **Surface 4 — Nested Hierarchy** (`/org/hierarchy`): the reseller →
    customer → subtenant tree, a vertical collapsible view.
  - **Surface 5 — White-Label Branding** (`/org/reseller/branding`) plus the
    reseller-settings stub nav (General / Billing & Plans / API & Webhooks /
    Audit Log).
  - **Sub-customer onboarding wizard** (`/org/customers/new`): 3-step
    provisioning flow for a new customer org.
  - **Tenant switcher**: a sidebar dropdown in the customer-detail context to
    move between the reseller and its customers.
  - **Per-org management tabs** (`/org/customers/:id/{mcps,users,usage,tools,
audit,billing,settings}`): the seven customer-detail tabs a reseller uses
    to administer one customer org. Usage loads live from the reseller-scoped
    customer-dashboard endpoint (`:id`-ownership-checked); the other six are
    mock-data-first behind documented Track A swap-in seams.
    All surfaces are mock-data-first except the Surface 2 analytics; consequential
    actions (create customer, onboard) render disabled behind documented Track A
    swap-in seams.
- **`errorRate` on `UsageSummary` + Error Rate dashboard stat card.**
  `getUsageSummary()` now computes `errorRate` — the fraction (0–1) of
  `request_log` rows in the org/date window whose HTTP `status_code` is an
  error (`>= 400`), `0` when there is no traffic (`NULLIF` guards the
  divide-by-zero). The operator team dashboard gains an "Error Rate" stat
  card. The Track C S2 customer-detail frame's Error Rate (7d) card reads the
  same field. Regression guard:
  `src/dashboard/usage-summary-error-rate.integration.test.ts`.

### Removed

- **Dead `MICROSOFT_CLIENT_ID` / `MICROSOFT_CLIENT_SECRET` bicep config.**
  `gateway-app.bicep` set `MICROSOFT_CLIENT_ID`/`MICROSOFT_CLIENT_SECRET` env
  vars from `main.bicep` params. `config.ts` reads them only as fallbacks —
  `azureClientId = AZURE_AD_CLIENT_ID ?? MICROSOFT_CLIENT_ID ?? ''`,
  `azureClientSecret = AZURE_AD_CLIENT_SECRET ?? MICROSOFT_CLIENT_SECRET ?? ''`
  — and `AZURE_AD_CLIENT_ID`/`AZURE_AD_CLIENT_SECRET` are _unconditionally_
  provided by `gateway-app.bicep` (KV-backed `secretRef`s) on every deploy
  path, so the `MICROSOFT_*` fallback branch is never reached. Removed both
  env vars and both bicep params (`microsoftClientId`, `microsoftClientSecret`)
  from `main.bicep` + `gateway-app.bicep`. No deploy path set these params, so
  this is a pure dead-config removal. (`config.ts` keeps the now-unreachable
  `?? process.env.MICROSOFT_CLIENT_*` fallback as harmless dead code — a
  separate app-code cleanup, out of scope here.)

- **Interim `Install Azure CLI` step in `deploy.yml`.** The deploy job ran an
  apt-install of a version-pinned `azure-cli` because the `[self-hosted,
wyre-private]` runner pool ran the bare `myoung34/github-runner` image, which
  ships no `az`. The pool has since rolled onto the `wyre-aca-runner` image
  (`task_1779172079827`), which bakes in the Azure CLI and Bicep, so the
  install step is dead weight — `azure/login` and `azure/arm-deploy` find `az`
  on PATH directly. Removed the step and its comment block.

- **`azure/CONDUIT-PROD-DEPLOY.md` — `conduit.wyre.ai` cutover sequence
  pre-documented.** The cutover (runbook step 9) is now expanded into the six
  precise commands that run at Aaron's launch-window greenlight — RECORD 2 add
  → delete the auto-pending cert from the pre-bind attempt → create a
  controlled-name managed cert matching the bicepparam → bind → validate. Two
  traps documented inline: (a) the pre-bind ACA auto-cert ships with a random
  `-NNNN` suffix that does NOT match the bicepparam's `managedCertName`, so
  letting it provision would fail the next bicep deploy on a customDomain
  binding referencing a nonexistent cert — delete + recreate with the
  controlled name keeps bicep-as-source-of-truth; (b) the controlled-name cert
  uses HTTP-01, which only succeeds once RECORD 2 points the domain at the
  env, so the cert resource is created strictly AFTER RECORD 2 lands. One
  controlled session, not stretched.
- **`azure/vendor-fleet-completeness-check.sh` — probe-port note.** Added a
  comment block noting that a Job-to-CA same-env MCP probe must use port 80
  (the CA's internal HTTP ingress), NOT the container's targetPort `:8080`.
  Discovered 2026-05-21 when the Piece-1 acceptance probe against `:8080`
  returned `http=000` for all 33 — a false 0/33 signal that looks like total
  fleet outage but is actually Jobs not having direct-targetPort access (only
  CA-to-CA does — the gateway's `vendor-config.ts` default
  `http://<slug>-mcp:8080` is correct for the gateway and stays).

### Added

- **`/org/customers` page-handler reads real customer-org list — closes the
  customer-wizard E2E discontinuity (A-MVP).** The `/org/customers` Track C
  Surface 1 (reseller dashboard) now reads from
  `orgService.getCustomersOfReseller(org.id)` instead of the
  `cust_mock_*` placeholder set. Reseller-OWNERSHIP-scoped by construction
  (data-source enforces `parent_org_id === resellerId AND type='customer'`;
  route gate `requireResellerAccess` requires both `org.type === 'reseller'`
  AND caller-membership on the reseller — warden Finding 2). A reseller
  who creates a customer via the `/org/customers/new` wizard (LIVE since
  PR #229) now sees that customer in the list immediately — closes the
  customer-wizard-E2E discontinuity at the natural next step of the flow.
  Shape mapping `Organization` → `ResellerCustomer`: id / name / plan
  direct; `subdomain` is `slugify(name)` at display-time (path-based per
  current architecture, not stored — a separate Aaron-product-decision
  task tracks the path-vs-subdomain routing question); derived stats
  (`userCount` / `mcpCalls30d` / `lastActivity`) render as **em-dash
  placeholders** — never fabricated numbers alongside real id/name.
  Visibility-distinct-by-design per the F3 lesson + obvious-over-
  compelling rule (PR #233 + #235 audit). A separate full-triangle PR
  (per-customer-stats aggregator service `getCustomerStatsForReseller`)
  is tracked to replace the em-dashes with real counts once Aaron probes
  staging and the access-check shape is wardenable. The template's
  `ResellerCustomer.userCount` / `mcpCalls30d` / `lastActivity` are now
  `number | number-or-null` to encode the placeholder state structurally.
  Single DB read on the hot path (no `Promise.all` — `#196/#199`
  hang-class discipline preserved).

### Fixed

- **Color-consistency PR-2 — sweep hardcoded color literals onto the
  canonical `--text-on-*` / `--warning-text` / `--error` / `--accent-text`
  tokens (Tier-2 token-drift sweep).** Follow-up to PR-1 (#238). PR-1
  landed the three new `--text-on-accent` / `--text-on-success` /
  `--text-on-warning` tokens in `THEME_VARS` as the SoT for text rendered
  on bright fills; this PR sweeps the ~25 sites across 18 files that
  hardcoded `#0a0a0a` / `#fff` / `#00C9DB` / `#f59e0b` / `#dc2626` /
  `#10b981` onto the canonical tokens. Added `--text-on-error` to round
  out the family (the four bright fills — accent / success / warning /
  error — all get a paired text token now). The sweep:
  - `color: #fff` on `var(--accent)` buttons → `var(--text-on-accent)`
    (~12 sites incl. .btn-connect / .btn-upgrade in styles.ts, every
    primary CTA across team-\* templates). **Visible improvement**: white
    text on bright cyan was ~1.7:1 contrast (WCAG fail); near-black on
    bright cyan is ~12:1 (AAA pass). The drift-fix is also a contrast-fix.
  - `color: #0a0a0a` on `var(--accent)` step-dots / buttons → `var(--text-on-accent)`
    (~10 sites incl. reseller-{onboard-mcp,new-customer,customers,
    customer-tabs,customer-detail,branding}.ts).
  - `color: #0a0a0a` on `var(--success)` step-done dots → `var(--text-on-success)`
    (matches the bg semantic).
  - `background: #0a0a0a` on switch-thumbs over `var(--accent)` →
    `var(--text-on-accent)` (semantic: thumb is the "contrast disc" on
    the accent track).
  - `#f59e0b` warning + `#dc2626` error + `#10b981` recovered-green
    literals in `team-billing.ts` / `vendor-health-view.ts` /
    `team-connections.ts` / `profile-settings.ts` → `var(--warning-text)` /
    `var(--error)` / `var(--success-text)` (semantic-closest tokens).
  - `color: #00C9DB` in `.invite-banner` strong/code → `var(--accent-text)`.
  - One intentional theme-independent literal **kept** with explanatory
    comment at `reseller-onboard-mcp.ts` (vendor-icon-tile text — the bg
    is per-vendor vivid via `safeCssColor()`; white-on-vivid is the
    cross-theme correct pairing). Same family as the
    obvious-over-compelling rule (#235): when an exception is necessary,
    NAME it at the site rather than hide it.
  - One literal **deferred** with explanatory comment at
    `team-members.ts:.role-badge.admin` (`#c084fc` purple — no canonical
    `--role-*` token in `THEME_VARS` yet; defining the role-color palette
    is a separate task bundled with PR-3 native-dialog as a UX-tokens
    refactor surface). Per partial-implementation-is-worse-than-full-defer.

  Verification: 1253/1253 tests pass (incl. 9/9 `styles.test.ts`
  regression guards); tsc clean. No new tests beyond the existing
  PR-1 regression guards — the drift-sweep is structurally backstopped
  by the global rules + token-SoT that PR-1 landed.

- **Color-consistency PR-1 — global anchor + native-form-element +
  focus-visible resets land via the canonical theme system.** Pre-Aaron-
  staging-probe color-consistency audit caught two Tier-3 actively-
  broken classes leaking through browser defaults regardless of theme:
  (1) NO global `a {}` rule in `src/web/styles.ts`, so every unstyled
  anchor rendered the browser-default dark-blue (Aaron's "dark blue
  for links" complaint); (2) NO global `input/select/textarea {}`
  reset, so native form elements rendered white in dark mode regardless
  of theme (Aaron's "dialog boxes in dark mode using very light colors"
  complaint, in-product-modal-with-form-inputs class). Plus a missing
  keyboard `:focus-visible` outline for WCAG 2.4.7. Added the three
  globals to `PAGE_STYLES` routed through canonical tokens
  (`var(--accent-text)` / `var(--bg-input)` / `var(--text-primary)` /
  `var(--border-primary)` / `var(--accent)`). Per-component overrides
  still win via specificity; these globals only catch otherwise-
  unstyled elements. Three new tokens in `THEME_VARS`:
  `--text-on-accent` / `--text-on-success` / `--text-on-warning` —
  single SoT for the text-color rendered on top of bright fills (PR-2
  will sweep hardcoded `#0a0a0a` / `#fff` usages onto these tokens).
  Pattern-asserted in a new `src/web/styles.test.ts` regression guard
  so future drift fails CI rather than ships silently. Native-dialog
  (`alert()` / `confirm()`) replacement is filed as a separate full-
  triangle task (`task_1779495584592`) per the partial-implementation-
  is-worse-than-full-defer discipline.
- **Reseller customers list seam comments reframed — data source has shipped,
  page-handler not yet wired.** `reseller-customers.ts` file header + the
  in-page `ia-shell-note` claimed "renders mock data until the Track A
  customer-list endpoint lands" / "until Hank's Track C customer-list
  endpoint lands, then the data source swaps." Tier-2 HALF-STALE: the
  data source `OrgService.getCustomersOfReseller` has shipped (used by the
  reseller routes at `src/reseller/routes.ts`), but the `/org/customers`
  page-handler at `src/web/routes.ts` still passes a mock `cust_mock_*`
  set to this template. Reframed both seam comments to name (a) what
  shipped (the data source) and (b) what is the actual remaining gap
  (the page-handler wiring), so the next consumer-author has the full
  current picture. Wiring the page-handler to the real data source is a
  separate follow-up PR — out of scope for this audit-only pass.
- **Reseller new-customer wizard file-header refreshed — was claiming
  DISABLED-state when wizard is LIVE end-to-end.** `reseller-new-customer.ts`
  header (lines 14-19) read "the final 'Create customer' CTA is DISABLED —
  there is no provisioning endpoint yet. That disabled CTA is the
  documented swap-in seam: when the Track A customer-provisioning endpoint
  lands, the wizard POSTs and the CTA activates." All four claims are
  factually wrong post-`#229`: the endpoint shipped at
  `src/reseller/routes.ts:497`, the owner-invite email pipeline shipped
  alongside it, the wizard CTA is already live (Step-3 button carries
  `data-create-url`; the page-script `await fetch`es it at line 255), and
  the wizard genuinely creates customer orgs end-to-end. Comment lags
  state by an entire feature. Rewrote the header to name the LIVE state
  - cite the receiving endpoint + name what the page-script does on
    submit. Pure documentation refresh — no code path altered; wizard
    behavior unchanged. Same comment-rot-as-trust-erosion class as PR
    #234; same audit-driven-N-PR-closure cadence post-Layer-1-merge.
- **Reseller customer-detail Overview seam comments refreshed to current
  reality — comment-rot trust-erosion class caught at the post-Layer-1
  audit moment.** The `reseller-customer-detail.ts` file header carried
  three stale claims that drifted out of sync with what actually shipped:
  (1) "UsageSummary has no errorRate field yet (confirmed not in any
  branch)" — factually wrong; `errorRate` lives at `dashboard-service.ts`
  and is guarded by an integration test, and the client already correctly
  consumes `usage.errorRate`. (2) "Track A customer-list/detail endpoint
  has not shipped" — the customer LIST endpoint has shipped (see
  `getCustomersOfReseller` in `src/reseller/routes.ts`); only the
  single-customer-detail endpoint is the actual remaining gap. (3) "MCP
  wiring-type, per-connection status dot, last-call — need the
  connection-config + vendor-health read models (PR #127)" — the
  owner-scoped vendor-health read model has landed (`assembleOrgVendorHealth`
  at `/api/orgs/:orgId/vendor-health`); only a reseller-scoped variant
  (`requireResellerOrCustomerAccess`-gated) is the actual remaining gap.
  Seam comments rewritten to name (a) the canonical-side that exists and
  (b) the missing-route + access-check surface, so the next consumer-author
  has the full SWAP-IN contract per the discipline ruby refined for #233.
  Pure comment-only change — no code paths altered.
- **Reseller customer-detail Billing tab no longer renders fabricated
  financial data — F3 lesson applied to the reseller-viewing-customer
  direction.** The Billing tab previously rendered a mock "Business — $49
  / user / month · 12 users" plan card with three mock invoices ($588.00 /
  $588.00 / $539.00). Post-Layer-1 audit caught this as a fresh instance
  of the F3 trust class ("render a number that can disagree with the real
  source") — once Layer 1 §8 PR-A landed, a reseller exercising staging
  would see "$49/user/month" on the customer Billing tab while the actual
  Stripe bill renders the composed `$600 base + N × $20` Layer 1 shape.
  The trust class is INVARIANT under render direction: a reseller making
  pricing/dunning/churn decisions about a customer on a fabricated number
  is the same disagreement-with-source-of-truth class as the customer
  seeing a fabricated card on their own /org/billing. Replaced with an
  honest empty state ("Live customer billing data lands when the reseller
  customer-billing endpoint ships. This tab will surface seat composition,
  monthly total, and invoice history scoped to this customer once that
  endpoint is wired."). Mock-data fields (`billingPlan` / `billingRate` /
  `invoices`) + the `InvoiceRow` type + the `.cdt-card-*` / `.cdt-inv-*`
  CSS are dropped — when canonical lands, legacy goes (same disposition
  shape as the `seat-billing.ts` deletion in PR-A). Seam comment updated
  to name BOTH the canonical service entry (`seatService.getSeatBilling`)
  AND the missing reseller-scoped READ ROUTE + access-check surface, so
  the next consumer-author has the full SWAP-IN contract.
- **`conduit-prod` deploy no longer fails on empty gateway-state JSON.** The
  `Read existing gateway state` step queried the existing container app's
  custom domains / `VENDOR_URL_*` vars and wrote them to temp files passed to
  the Bicep deploy via `--parameters @file`. On a fresh stack with no custom
  domains, `az`/`jq` yields an empty string, so `printf` wrote an **empty
  file** — and `az deployment group create` then failed with `Failed to parse
string as JSON: Expecting value: line 1 column 1 (char 0)`. The step now
  defaults both values to `[]` when empty (and `jq` tolerates a null `env`
  array via `(. // [])[]`), so a fresh-stack deploy passes valid empty JSON.
- **`microsoft-graph` / `m365` vendor OAuth now has real client credentials.**
  Both vendors' `oauthConfig.clientIdEnv` reads `MICROSOFT_CLIENT_ID` /
  `MICROSOFT_CLIENT_SECRET`, but no deploy path ever set those env vars (the
  bicep params defaulted to empty string and were later removed as "dead
  config") — so `buildAuthorizeUrl` would throw `Missing environment
variable` on any connect attempt. A multi-tenant Entra app registration
  ("Conduit Graph Enterprise MCP Connector", appId
  `7dd3420d-a28f-4cc3-83e8-29113663c7b6`) now backs both vendors; its
  client id + secret are stored as `microsoft-client-id` /
  `microsoft-client-secret` in each environment's Key Vault, and
  `gateway-app.bicep` wires them into the container app as KV-backed
  `secretRef` env vars — the same pattern `AUTH0_CLIENT_*` uses. The Entra
  app holds all 41 read-only `MCP.*` delegated scopes; each customer tenant
  still grants admin consent out of band when it connects.
- **`/org/billing` no longer shows fabricated billing data.** The billing page
  rendered a hardcoded mock payment method (`visa •••• 4242`), a mock upcoming
  invoice, four fake invoices, and a `creditsUsed` figure derived as 37% of the
  allocation. `creditsUsed` is now the real value from
  `CreditService.getUsageThisMonth(org.id)` (sum of `credit_ledger` since the
  month start). The mock payment-method / next-invoice / invoice-history
  sections are replaced by a single honest **Billing details** block: an org
  with a Stripe customer gets an _Open billing portal_ button (`POST
/api/billing/portal` → the customer's real Stripe portal, which natively
  shows their real card, invoices, and upcoming charge); an org with no Stripe
  customer gets an honest "billing is managed directly" state with no button.
  No fabricated billing data reaches the page. On-page invoice rendering
  remains deferred to the Layer 1 §8 per-seat billing work.
- **`tools/call` on the unified `/v1/mcp` endpoint hung — `Promise.all` of
  service-method calls stalled the request reserved-tx connection.** Every
  `tools/call` reached `shouldCapturePrompt`, whose
  `await Promise.all([billingGate.canUsePromptCapture(orgId), orgService.getPromptCaptureEnabled(orgId)])`
  issues two concurrent DB queries on the request's single reserved
  transaction connection — which stalls it (TLDIAG-localized: the tool-call
  handler entered `shouldCapturePrompt` and never returned). Same hang class
  as the `tools/list` bug (PR #196); the precise postgres.js cause remains
  uncharacterized (tracked post-launch). The fix is mechanism-independent —
  sequentializing such a `Promise.all` into awaited calls is correctness-
  preserving and removes the concurrency. Six sites carrying that shape on
  the reserved-tx path are sequentialized: `shouldCapturePrompt`
  (`audit/prompt-capture.ts`), the inline prompt-capture gate in the audit
  page (`web/routes.ts`), `getAllToolAllowlists` (`org/tool-allowlist-service.ts`),
  the team-credential check in `oauth/authorization-server.ts` (now a single
  set-based query), the reseller-detail route (`reseller/routes.ts`), and
  `CreditService.getTotalAvailable` / `hasCreditsRemaining`
  (`billing/credit-service.ts`). `Promise.all`s of raw `sql\`\``query
objects (the postgres.js-idiomatic, pipelined pattern — e.g. the dashboard
and audit-list queries) are unaffected and left as-is. This supersedes
PR #196's flat-vs-nested framing: the operative axis is the shape of the`Promise.all` members, not its arity.
- **Reseller console — subtenant UI hardening pass.** A QA sweep of the
  merged subtenant surface (reseller console, both wizards, the 7 per-org
  tabs) fixed the unambiguous defects. Security: hex colors bound into
  `style=""` contexts (branding swatches, onboard-mcp catalog icons) now
  validate via a new `safeCssColor` helper rather than HTML-escape only —
  `escapeHtml` does not neutralize a CSS context; logo `src` URLs are
  https-scheme-checked (`safeHttpsUrl`); the hierarchy tree walk carries a
  visited-set + depth cap so a cyclic or pathologically deep Track A tree
  cannot overflow the stack. Correctness: the wizard step coercers reject
  `parseInt`-salvageable garbage; an unknown customer-tab renders a neutral
  body instead of the editable-looking Settings form; Settings inputs are
  read-only until their write endpoint lands. Accessibility: ARIA tree roles
  on the hierarchy, `aria-current` on wizard steppers, `th scope`, screen-
  reader state text on tool-access checkboxes, dropped a misleading
  `role="menu"` from the tenant switcher. Plus responsive fixes (mobile
  table labels, narrow-viewport steppers) and amber/red hex → theme tokens.
- **Request-context: connection leak on client disconnect during `reserve()`,
  and unbounded `reserve()` wait on pool exhaustion.** The request-context
  plugin's `onRequest` hook registered its raw-socket-`close` listener AFTER
  `await openRequestContext`, so a client that disconnected during the
  `reserve()` acquire window left the eventually-acquired reserved connection
  with no release path — `onResponse` never fired because the response never
  completed. The listener is now registered synchronously BEFORE the await;
  pre-acquire closes set an `abortedEarly` flag that the post-await check
  honors by releasing the late-arriving handle immediately (idempotent with
  the existing settle-once guard). Separately, `postgres.js`'s `reserve()`
  has no acquire timeout, so a pool-exhausted gateway would hang every new
  request forever. `openRequestContext` now races `reserve()` against a 5s
  timer (`RESERVE_TIMEOUT_MS`); on timeout it throws a typed
  `RequestPoolBusyError` which the plugin maps to HTTP 503 — fail loud, not
  hang. The losing reserve promise is attached with `.then(r => r.release())`
  so a late acquisition does not silently strand a pool slot.
- **`tools/list` on the unified `/v1/mcp` endpoint hung indefinitely.** The
  `aggregateTools` Phase-1 vendor discovery fanned out a per-org/per-team
  `Promise.all` of DB queries onto the request's single reserved-transaction
  connection; a nested / dynamic-fan-out of concurrent queries on that
  connection deadlocks (flat-fixed concurrent `Promise.all`s on the same
  connection are unaffected — the precise postgres.js cause is tracked
  separately as a post-launch investigation). Phase 1 is now a fixed, small
  number of sequential set-based queries (`listOrgVendorsForOrgs`,
  `getUserTeamIds`, `listTeamVendorsForTeams`) — `O(1)` query count instead of
  the previous `O(orgs × teams)` N+1. Phase 2 is restructured into three
  passes: sequential credential resolution, a concurrent (DB-free) HTTP tool
  fetch, then sequential allowlist filtering — removing all concurrent DB
  queries while keeping per-vendor tool fetches parallel. The credential
  injector's team tier likewise replaces its per-team `Promise.all` fan-out
  with a single set-based `getTeamCredentialsForTeams` query.
- **Microsoft/Entra sign-in broken on staging — malformed double-scheme
  callback URL.** `params.staging.bicepparam` never set `allowedHosts`, so the
  staging gateway got `ALLOWED_HOSTS=''`. `config.ts` splits that to an empty
  allowlist; `getRequestBaseUrl()` then cannot match the request `Host` and
  falls back to its hardcoded `http://localhost:8080` literal, producing
  `redirect_uri=https://http://localhost:8080/auth/microsoft/callback` —
  Microsoft rejects it. Now sets `allowedHosts = 'staging.conduit.wyre.ai'`
  (bare host, no scheme). The production deploy already passes `allowedHosts`
  inline; staging never did.

- **Staging/production deploy failed — `azure/arm-deploy` strips quotes from
  inline JSON `--parameters`.** `existingCustomDomains` and `existingVendorEnv`
  were passed inline as `param=<json>`; `arm-deploy` strips the double quotes,
  so `[{"name":"x"}]` reached `az` as `[{name:x}]` — unparseable
  (`Failed to parse string as JSON`). The `Read existing gateway state` step now
  writes the JSON to files and the deploy steps pass them as `param=@file`,
  keeping the JSON off the command line so quotes survive. Fixes both the
  staging and production deploy steps. Surfaced once the `BCP258` fix let the
  deploy reach template validation for the first time.
- **Staging deploy targets the existing managed environment.**
  `params.staging.bicepparam` now pins `containerEnvName = 'mcpgw-staging-env-v2'`.
  Unset, `main.bicep` resolved the staging managed-environment name to the
  bicep-canonical `mcpgw-staging-env`, so a deploy would have _created a new
  managed environment_ and re-homed the gateway onto it. The live staging stack
  runs on the hand-built `mcpgw-staging-env-v2`; pinning it keeps a deploy an
  in-place revision bump. Mirrors the production step's existing
  `containerEnvName=mcpgw-prod-env-v2` pin.

- **Staging deploy pipeline never worked — `BCP258` on the bicepparam.**
  `params.staging.bicepparam` and `params.conduit-prod.bicepparam` omitted the
  four `@secure` params (`masterKey`/`jwtSecret`/`pgPassword`/`ghcrToken`) on
  the false assumption that CI `--parameters` overrides would fill them. A
  `.bicepparam` file must self-assign every required (non-defaulted) param of
  its `using` target — CLI overrides can only override params it already
  assigns. Both files now assign the four via `readEnvironmentVariable()`;
  `deploy.yml`'s staging step supplies them as `env:` vars from the staging
  GitHub environment secrets, and the conduit-prod Phase-B runbook exports them
  before `az deployment`. This is why every `deploy.yml` staging run had failed.

- **Docs site never shipped in the gateway image — `/docs` 404'd.** `src/index.ts`
  serves the Astro Starlight docs site from `public/` via `@fastify/static`
  (registered only when `public/` exists, so a docs-less image boots clean),
  and the site builds with base path `/docs`. But the `Dockerfile` never built
  the docs or copied them in — `public/` is gitignored and was absent from
  every image, so `existsSync(publicDir)` was always false and `/docs` 404'd.
  CI builds the docs (the `docs-build` check, 26 pages) only to discard the
  artifact. Added a digest-pinned `docs-builder` stage to the `Dockerfile` and
  `COPY --from=docs-builder /docs/dist ./public/docs`, so the gateway image now
  bundles and serves the documentation at `/docs`.

### Added

- **`deploy.yml` guard step — reject an unset or empty staging deploy secret.**
  `readEnvironmentVariable()` throws only on an _unset_ var; since CI always
  defines the `env:` vars, a missing GitHub-environment secret resolves to `''`
  and would otherwise compile a blank `@secure` param. The new "Assert staging
  deploy secrets present" step fails the deploy loudly before it runs if any of
  `MASTER_KEY`/`JWT_SECRET`/`PG_PASSWORD`/`GHCR_TOKEN` is unset or empty.

---

# [1.11.0](https://github.com/wyre-technology/conduit/compare/v1.10.0...v1.11.0) (2026-05-15)

### Bug Fixes

- **billing:** reuse Stripe customer on resub + alert on webhook mismatch ([#96](https://github.com/wyre-technology/conduit/issues/96)) ([3686429](https://github.com/wyre-technology/conduit/commit/368642967db5cdebd1cffed79135a535d1919be7)), closes [#conduit-sales](https://github.com/wyre-technology/conduit/issues/conduit-sales)
- **ci:** tag release image from pre-release SHA, not release commit ([#93](https://github.com/wyre-technology/conduit/issues/93)) ([0044c60](https://github.com/wyre-technology/conduit/commit/0044c6039acf29cef7ab9f6d2d2360e3fada48e2))

### Features

- **billing:** dunning lifecycle Loops events + grace-aware service gate ([#94](https://github.com/wyre-technology/conduit/issues/94)) ([a415bef](https://github.com/wyre-technology/conduit/commit/a415befad3f93de323c2a1b8e5766c9ad3c4c79c)), closes [#10](https://github.com/wyre-technology/conduit/issues/10) [#10](https://github.com/wyre-technology/conduit/issues/10)
- **billing:** dunning UI extension — 5 render fns + tests + 14 preview fixtures (Track B follow-up) ([#95](https://github.com/wyre-technology/conduit/issues/95)) ([61d36c0](https://github.com/wyre-technology/conduit/commit/61d36c08127a08554eb9d770aa7c10ac0bf413dc)), closes [#92](https://github.com/wyre-technology/conduit/issues/92)
- **billing:** IA shell for /org/billing with mock data (Track B) ([#92](https://github.com/wyre-technology/conduit/issues/92)) ([9b58096](https://github.com/wyre-technology/conduit/commit/9b58096d17db28874f8b6197ad43a0d9b5001790)), closes [#73](https://github.com/wyre-technology/conduit/issues/73)
- **billing:** swap dunning from mock to real read-model (Track A integration) ([#98](https://github.com/wyre-technology/conduit/issues/98)) ([48f9c5d](https://github.com/wyre-technology/conduit/commit/48f9c5ddf2277b4008b8ac976e9de84f47f5e295))
- **billing:** Track C PR-A — reseller_pricing_config foundation ([#97](https://github.com/wyre-technology/conduit/issues/97)) ([96ac432](https://github.com/wyre-technology/conduit/commit/96ac43257550c9a35ea0a0981e0ba79164ad09b3)), closes [#10](https://github.com/wyre-technology/conduit/issues/10)
- **billing:** Track C PR-A follow-up — DP-E current-only + created_by strip ([#99](https://github.com/wyre-technology/conduit/issues/99)) ([666865f](https://github.com/wyre-technology/conduit/commit/666865f79c71e4bafa88d27279c4bd9a0834b04d)), closes [#10](https://github.com/wyre-technology/conduit/issues/10)

# [1.10.0](https://github.com/wyre-technology/conduit/compare/v1.9.0...v1.10.0) (2026-05-13)

### Features

- **db:** mig 023 — conduit_is_reseller_admin_of_ancestor recursive helper ([#91](https://github.com/wyre-technology/conduit/issues/91)) ([2553469](https://github.com/wyre-technology/conduit/commit/255346941e9c9cbe65e2e817c6281d31fecaa1cc)), closes [#10](https://github.com/wyre-technology/conduit/issues/10)
- **ia:** hoist /settings/team/_ + /settings/billing → /org/_ with permanent 301 redirects ([#90](https://github.com/wyre-technology/conduit/issues/90)) ([3f7ee19](https://github.com/wyre-technology/conduit/commit/3f7ee193285bbcff2de4aaa60502f501ca62e115))

# [1.9.0](https://github.com/wyre-technology/conduit/compare/v1.8.0...v1.9.0) (2026-05-12)

### Bug Fixes

- **billing:** route remaining plan-gate sites through isPaidPlan helper ([#87](https://github.com/wyre-technology/conduit/issues/87)) ([740a163](https://github.com/wyre-technology/conduit/commit/740a163fe15204f100981fb3df98cf0132f0276a)), closes [72/#86](https://github.com/wyre-technology/conduit/issues/86)
- **deps:** bump fast-uri 3.1.0 → 3.1.2 for two HIGH CVEs ([#85](https://github.com/wyre-technology/conduit/issues/85)) ([b6edbc4](https://github.com/wyre-technology/conduit/commit/b6edbc43f336152c4d96ddca3105d2fd958d194b)), closes [HI#severity](https://github.com/HI/issues/severity)
- **gate:** match handler-gate to render-gate via isPaidPlan helper ([#71](https://github.com/wyre-technology/conduit/issues/71)) ([1ce8ca9](https://github.com/wyre-technology/conduit/commit/1ce8ca9db5a9820036ca7d73ed0ac81e9d72906a)), closes [#9](https://github.com/wyre-technology/conduit/issues/9)
- **gate:** sweep plan-equality drift in dashboard + audit API gates ([#72](https://github.com/wyre-technology/conduit/issues/72)) ([335741c](https://github.com/wyre-technology/conduit/commit/335741c92318ba17451499c76be5764c602f2393)), closes [#71](https://github.com/wyre-technology/conduit/issues/71) [#10](https://github.com/wyre-technology/conduit/issues/10) [#10](https://github.com/wyre-technology/conduit/issues/10) [#71](https://github.com/wyre-technology/conduit/issues/71) [#71](https://github.com/wyre-technology/conduit/issues/71)
- **layout:** remove dead sidebar nav items pointing at unregistered routes ([#70](https://github.com/wyre-technology/conduit/issues/70)) ([3c0b7d1](https://github.com/wyre-technology/conduit/commit/3c0b7d11f7ed291b028cb860ab68b431a21a1318)), closes [#69](https://github.com/wyre-technology/conduit/issues/69)

### Features

- **db:** mig 021 — relax enforce_org_hierarchy to bounded-depth-3 ([#83](https://github.com/wyre-technology/conduit/issues/83)) ([a555d10](https://github.com/wyre-technology/conduit/commit/a555d1012b2dd4e0cab85cc7e7864db0a908ac33)), closes [67-#73](https://github.com/67-/issues/73) [#74-pending](https://github.com/wyre-technology/conduit/issues/74-pending) [#75](https://github.com/wyre-technology/conduit/issues/75)
- **db:** mig 022 — Bug B sweep on remaining 9 UPDATE policies ([#86](https://github.com/wyre-technology/conduit/issues/86)) ([94b2041](https://github.com/wyre-technology/conduit/commit/94b204197d37e551729f832305fc6a70d90c95a2)), closes [#10](https://github.com/wyre-technology/conduit/issues/10)
- **layout:** IA restructure A — Organization parent + sub-nav (5 items) ([#73](https://github.com/wyre-technology/conduit/issues/73)) ([3b5b1c2](https://github.com/wyre-technology/conduit/commit/3b5b1c2b77d44b2b415d3447560e77c3f7212c07)), closes [#74](https://github.com/wyre-technology/conduit/issues/74) [#74](https://github.com/wyre-technology/conduit/issues/74) [#72](https://github.com/wyre-technology/conduit/issues/72) [#10](https://github.com/wyre-technology/conduit/issues/10) [#70](https://github.com/wyre-technology/conduit/issues/70) [#10](https://github.com/wyre-technology/conduit/issues/10) [#74](https://github.com/wyre-technology/conduit/issues/74) [#71](https://github.com/wyre-technology/conduit/issues/71)

# [1.8.0](https://github.com/wyre-technology/conduit/compare/v1.7.0...v1.8.0) (2026-05-11)

### Bug Fixes

- **auth:** restore Microsoft sign-in by mirroring mcp-gateway env handling ([#68](https://github.com/wyre-technology/conduit/issues/68)) ([14d0847](https://github.com/wyre-technology/conduit/commit/14d08475ff756572029dd991c126f8f31d7ac4e3))
- **landing:** non-customer Sign In goes through provider chooser, not Auth0-direct ([#69](https://github.com/wyre-technology/conduit/issues/69)) ([444dc71](https://github.com/wyre-technology/conduit/commit/444dc7115a17bceddfa99c78ea6a2cf424b77529)), closes [#68](https://github.com/wyre-technology/conduit/issues/68) [#68](https://github.com/wyre-technology/conduit/issues/68)
- **migrations:** recover 013_organization_domains and 014_rls_with_check_clauses ([0a9e64d](https://github.com/wyre-technology/conduit/commit/0a9e64d40a3df1e430218e7e3755614dd201e121)), closes [#38](https://github.com/wyre-technology/conduit/issues/38) [#46](https://github.com/wyre-technology/conduit/issues/46)
- **rls:** migration 020 — helper-context fix (Bug A) + UPDATE-USING repair (Bug B partial) ([b2a0a0b](https://github.com/wyre-technology/conduit/commit/b2a0a0b0a08d453ee148f32f2aca632a29d89f83)), closes [#65](https://github.com/wyre-technology/conduit/issues/65)
- **rls:** SECURITY DEFINER helpers replace recursive RLS predicates (mig 018) ([5e677bf](https://github.com/wyre-technology/conduit/commit/5e677bff73ddfa6b2a7ca677868af1cf76d55d1c))
- **rls:** temporarily remove organizations_insert WITH CHECK pending root cause ([c6e02b3](https://github.com/wyre-technology/conduit/commit/c6e02b3eb59202fd1e61bdee1a537ef03b6383d7))
- **scim/integration-harness:** repair stale organizations bootstrap ([317545d](https://github.com/wyre-technology/conduit/commit/317545d86b6ed6f02352c8128710ff5831ae0a36)), closes [#46](https://github.com/wyre-technology/conduit/issues/46) [#46](https://github.com/wyre-technology/conduit/issues/46)

### Features

- **admin:** platform admin dashboard + orgs + reports ([#56](https://github.com/wyre-technology/conduit/issues/56)) ([bdc0dca](https://github.com/wyre-technology/conduit/commit/bdc0dca97409360e256fd32f0d437bfa46705339))
- **db/migrate:** assert numeric contiguity of migrations on boot ([4b487ca](https://github.com/wyre-technology/conduit/commit/4b487caf01cdbaa0f6a4df7e0cefbf8f0f8c05cf)), closes [#38](https://github.com/wyre-technology/conduit/issues/38) [#46](https://github.com/wyre-technology/conduit/issues/46)
- **migrations:** 015 drop plaintext invitation token column ([8f570b1](https://github.com/wyre-technology/conduit/commit/8f570b131980cf6277e0f52211ff7a30c73623d7))
- **scripts:** pre-migration backfill for invitation token_hash ([b1905d6](https://github.com/wyre-technology/conduit/commit/b1905d6ad8076ceae82a71faeb11f16da7766dd4))
- **ui:** adopt WYRE visual design (cyan accent, Oswald headings) ([#57](https://github.com/wyre-technology/conduit/issues/57)) ([3b11b5e](https://github.com/wyre-technology/conduit/commit/3b11b5e25d826a6e0723ba60f404752593338331)), closes [#00C9DB](https://github.com/wyre-technology/conduit/issues/00C9DB) [#2563eb](https://github.com/wyre-technology/conduit/issues/2563eb) [#EDE947](https://github.com/wyre-technology/conduit/issues/EDE947)
- **ui:** port legal pages (Terms, Privacy) ([#58](https://github.com/wyre-technology/conduit/issues/58)) ([3cc74dd](https://github.com/wyre-technology/conduit/commit/3cc74dd2cceb64e0f44e150d154aa721b1735cfa))

# [1.7.0](https://github.com/wyre-technology/conduit/compare/v1.6.1...v1.7.0) (2026-05-08)

### Features

- **auth:** multi-provider login (Auth0 + Microsoft Entra side-by-side) ([#54](https://github.com/wyre-technology/conduit/issues/54)) ([0365ee3](https://github.com/wyre-technology/conduit/commit/0365ee3db543781e44abf3568cf2529553fbe433))
- **loops:** wire user signup + org_created drips ([#50](https://github.com/wyre-technology/conduit/issues/50)) ([08431cd](https://github.com/wyre-technology/conduit/commit/08431cd5166c9a7a8df79b5a8f9575ed5361f30f))

## [1.6.1](https://github.com/wyre-technology/conduit/compare/v1.6.0...v1.6.1) (2026-05-07)

### Bug Fixes

- **ci:** skip SARIF upload from fork/dependabot PRs ([#49](https://github.com/wyre-technology/conduit/issues/49)) ([17e42dd](https://github.com/wyre-technology/conduit/commit/17e42dd012e470de1b5928d4768781dfa6f85674))

# [1.6.0](https://github.com/wyre-technology/conduit/compare/v1.5.0...v1.6.0) (2026-05-06)

### Features

- **audit:** prompt-capture toggle on /settings/team/audit ([3e44050](https://github.com/wyre-technology/conduit/commit/3e4405032785eafde28ac3a6ebfcae53ce4db7ba))
- **audit:** wire prompt capture correctly on both proxy paths ([9e89402](https://github.com/wyre-technology/conduit/commit/9e89402e23e753cac5bff9010d0956b9bd0a850e))

# [1.5.0](https://github.com/wyre-technology/conduit/compare/v1.4.0...v1.5.0) (2026-05-05)

### Bug Fixes

- **docker:** copy migrations/ into runtime image ([af7e628](https://github.com/wyre-technology/conduit/commit/af7e6286048dd4d7d48a1af38155e42126f00540))
- **migrate:** tolerate pre-mig-011 org_invitations on source ([93b05b7](https://github.com/wyre-technology/conduit/commit/93b05b75c5e9bbe1c4a593d31ff6b780c82b63f9))

### Features

- **consolidation:** fold mcp-gateway into Conduit ([#46](https://github.com/wyre-technology/conduit/issues/46)) ([4e8cf87](https://github.com/wyre-technology/conduit/commit/4e8cf878854c06655a660ad7da0572b323718d13))
- **db:** migration runner — apply migrations/\*.sql at boot ([ffd529a](https://github.com/wyre-technology/conduit/commit/ffd529a07e447f0bd80b2b235ad76d05f2789e91))
- **deploy:** staging.conduit.wyre.ai auto-deploy path ([0d722b7](https://github.com/wyre-technology/conduit/commit/0d722b73e40667803cc89096ae7af8383f2ef011))
- **scim:** SCIM 2.0 inbound provisioning (tenant + reseller scope) ([#38](https://github.com/wyre-technology/conduit/issues/38)) ([400c9a0](https://github.com/wyre-technology/conduit/commit/400c9a07536c893c2a05589bf7887b4319c9e6e4))
- **security:** backport safe-fetch SSRF guard for ported vendors ([25a80c6](https://github.com/wyre-technology/conduit/commit/25a80c6e4796a97fa609d93a25cde171723ee4d6))

# [1.4.0](https://github.com/wyre-technology/wyre-mcp-gateway-platform/compare/v1.3.4...v1.4.0) (2026-04-21)

### Bug Fixes

- **test:** align brand tagline expectation with 'Customer MCP Gateway' ([#21](https://github.com/wyre-technology/wyre-mcp-gateway-platform/issues/21)) ([f9fa99b](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/f9fa99bd1082ab33d173239509b4b6316c2a821d))

### Features

- **brand:** BrandResolver with hierarchical org→parent→wyre-default ([6628724](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/6628724602d699eb0f8cae8af1342e3c0d75dbbb))
- **brand:** extend BrandConfig type for white-label schema ([501f736](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/501f736f5ff714a99ad6a9f1f28bfcb369bd9b27))
- **credentials:** reseller-shared credential resolution fallback ([7a3f04b](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/7a3f04b60f159b99e2929ffa7e5fac3ec9876d79))
- **docs:** Astro Starlight shell for Conduit documentation ([cda1378](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/cda13786df18ea8e66e523a11f9a5dcc7df0a88f))
- **migrations:** brand_profiles + white-label schema (008) ([b61bc0b](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/b61bc0bd9df45b5610d770911fd81bd8838012b6))
- **migrations:** impersonation_sessions + reseller_admin_audit (012) ([8749912](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/8749912344d46c851edaa75c1dd12ce8aff5f997))
- **migrations:** onboarding_progress + customer sub-org schema (009-010) ([c529d76](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/c529d76cd38e80a64e624c43ebe475163b3ff667))
- **migrations:** reseller tenancy schema + RLS (002-007) ([64ffd0a](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/64ffd0a54f5ec45c399ec51bcb315feb286f3df5))
- **org:** hash invitation tokens at rest (dual-write) ([804880a](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/804880a4cc474c4d433a92c7c81cb1b2b3b76974))
- **org:** reseller hierarchy helpers on OrgService ([b0891ea](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/b0891eaa6f41f27c0cd29ae681e1c92ae75b514b))
- **org:** ResellerMemberService with §5.2 permission matrix ([84892f0](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/84892f06978b5e7c1ce6a9ad048f366378bc3209))
- **platform-ops:** upstream sync tooling + release pipeline ([707e6d0](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/707e6d0b0e27e7177bce17180a2eb6c68cafac56))
- **proxy:** credential-injector uses reseller-shared fallback ([7b59fc7](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/7b59fc7a19c736dd2dc876e9bd85790aa3896d62))
- **reseller:** CRUD endpoints for reseller profile + members ([dc01763](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/dc01763003139b27b59db382aa1e377e5a0f6b2f))
- **reseller:** membership-gated Or404 lookups on ResellerService ([70fca5b](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/70fca5b85270df0b8f614eabd5234980e0659db3))
- **reseller:** middleware helpers for role + cross-tenant access ([1d05877](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/1d05877df305ae1fda7e698afecbb294a4045f87))
- **reseller:** scaffold reseller admin package behind feature flag ([8620154](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/862015413ac73867a708285f0029f25a4f7fb9c7))
- **security:** baseline security scanning (CodeQL/Deps/Gitleaks/Trivy) ([56a6891](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/56a6891fef53adfeba11a95c7d2faf50e5342926))
- **signup:** public /signup route with Auth0 login_hint + rate limit ([d4c8848](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/d4c884804de21c5415ef2f13325300dc106df1dd))
- **taskmaster:** initialize Conduit task backlog from 8 PRDs ([5af5fc7](https://github.com/wyre-technology/wyre-mcp-gateway-platform/commit/5af5fc7089ad01da3427deb1db95461516b0196b))

# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## Provenance

This changelog tracks Conduit-specific changes. Conduit is downstream of
wyre-technology/mcp-gateway — inherited upstream changes are summarized
here rather than re-itemized. For the full upstream history see the
mcp-gateway CHANGELOG.

## [Unreleased]

### Conduit-specific

#### Added

- Reseller/MSP multi-tenancy expand migration (`migrations/002_reseller_tenancy_expand.sql`)
- Upstream sync automation: `scripts/sync-upstream.sh` and `.github/workflows/upstream-sync-report.yml`
- Taskmaster integration (`.taskmaster/`) for platform-ops planning
- CODEOWNERS for review routing
- Operations documentation (`docs/operations/`)
- Security baseline scanning (platform-ops #13): Dependabot config
  (`.github/dependabot.yml`), CodeQL workflow, gitleaks workflow,
  Trivy filesystem + container image workflow, and
  `docs/operations/security-scanning.md` triage guide. All findings
  upload as SARIF to the GitHub Security tab; non-blocking by design.
- `docs/security.md` — initial customer-facing security documentation
  covering credential encryption, trust model (with org-standard
  anti-claim language on certifications), authentication, multi-tenant
  - subtenant isolation, prompt injection defenses, billing/dunning
    security, and audit logging.
- `docs/deploying.md` — Cloudflare Pages deployment guide for the docs
  site under `conduit.wyre.ai/docs`.
- `docs/src/content/docs/reference/subtenants.mdx` — customer-facing
  reference page for the subtenant model: depth-3 reseller hierarchy,
  the two disjoint role spaces (org-member vs reseller-scope), the
  transitive-admin recursive helper, vendor-credential downstream
  sharing via reseller-shared grants, reseller-scoped pricing config
  with subtenant-opaque visibility, and clean severance behavior.
  Sidebar-linked under **Reference**.

#### Changed

- Docs site is now configured for the public path
  `https://conduit.wyre.ai/docs` (Astro `site` + `base: '/docs'` in
  `docs/astro.config.mjs`). Previously a placeholder
  `https://docs.conduit.example`.
- Migrated security documentation into the Starlight content tree at
  `docs/src/content/docs/reference/security.mdx`. The legacy
  `docs/security.md` was a draft markdown file that the Starlight build
  did not serve — customers could not read it at
  `conduit.wyre.ai/docs`. The Starlight version is now sidebar-linked
  under **Reference**. The legacy file is retained as engineering
  reference with a "MIGRATED" pointer at the top, pending the broader
  legacy `docs/*.md` sweep-delete.
- Migrated the MSP onboarding guide into the Starlight content tree at
  `docs/src/content/docs/guides/msp-onboarding.mdx` (previously a
  "Coming soon" stub). Content rebranded from "MCP Gateway platform" to
  Conduit, and UI paths corrected for the `/settings/*` → `/org/*` IA
  hoist (PRs #57/#73/#90) — verified against the registered routes in
  `src/web/routes.ts`. The legacy `docs/onboarding-guide.md` is retained
  as engineering reference with a "MIGRATED" pointer.
- Migrated the API reference into the Starlight content tree at
  `docs/src/content/docs/reference/api.mdx` (previously a "Coming soon"
  stub). Endpoint paths verified against the registered routes in `src/`
  (org/oauth/health/audit/tool-access/billing/proxy). Added a note that
  billing endpoints are conditionally registered (Stripe-dependent) and
  scoped the section as subscription billing so reseller-channel billing
  can be added later without a rewrite; documented the
  `POST /api/webhooks/stripe` ingest endpoint. The legacy
  `docs/api-reference.md` is retained as engineering reference with a
  "MIGRATED" pointer.
- New `docs/src/content/docs/guides/billing.mdx` — customer-facing
  Billing & Plans guide: the Free/Pro/Business plan comparison (built
  from `src/billing/plan-catalog.ts`), credit model, upgrade paths,
  subscription management, and the dunning lifecycle (payment-failing →
  past-due → final-warning → suspended, with the 7-day grace window).
  Sidebar-linked under **Guides**.
- New `docs/src/content/docs/guides/scim.mdx` — customer-facing SCIM
  Provisioning guide consolidating the four legacy `docs/scim/*.md`
  IdP guides (Entra ID, Okta, JumpCloud, Google Workspace) into one
  page: shared token-generation steps, per-IdP setup, the
  what-gets-provisioned mapping, and per-IdP troubleshooting. UI paths
  corrected to `/org/scim` (verified against `src/web/routes.ts`). The
  legacy `docs/scim/*.md` files now carry "MIGRATED" pointers.
  Sidebar-linked under **Guides**.
- New Reference pages migrated from legacy `docs/*.md`, all sidebar-linked
  under **Reference**:
  - `reference/architecture.mdx` — system architecture, component roles,
    and the MCP-tool-call data flow. States the production deployment
    target (Azure Container Apps, verified against `.github/workflows/
deploy.yml` + `azure/main.bicep`).
  - `reference/cli.mdx` — the CLI wrapper endpoint (`/v1/:vendor/cli`):
    session pooling, result caching, token savings, rate limits.
  - `reference/prompt-capture.mdx` — the audit-log privacy boundary:
    what is and is not captured (no prompt content, no tool arguments,
    no results).
    The legacy `docs/architecture.md`, `docs/cli-wrapper.md`, and
    `docs/prompt-capture.md` now carry "MIGRATED" pointers.
- Filled in `docs/src/content/docs/guides/vendor-connections.mdx`
  (previously a "Coming soon" stub) — the supported-vendor catalog,
  credential scopes, connecting/rotating vendors, and a
  connection-failure troubleshooting table.
- New `docs/src/content/docs/reference/vendor-health.mdx` — documents
  the tenant-scoped vendor-health feature: the four health states
  (healthy/degraded/down/unknown), the cached-polling model, the
  `GET /api/orgs/:orgId/vendor-health` endpoint, and what vendor
  health does and does not cover. Sidebar-linked under **Reference**.

#### Fixed

- Corrected the `/health/vendors` entry in the API reference. The doc
  said "No authentication required"; the endpoint is now admin-gated
  (`X-Admin-API-Key`) — it was hardened to remove a cross-tenant
  information-disclosure. Added the tenant-facing
  `GET /api/orgs/:orgId/vendor-health` endpoint, which the API
  reference did not previously list.
- Corrected the Pro-tier feature list in
  `docs/src/content/docs/guides/msp-onboarding.mdx`. The list (carried
  from the pre-Business-tier legacy guide) wrongly attributed audit log
  CSV export and service clients to the Pro plan; per
  `src/billing/plan-catalog.ts` those are **Business**-tier features.
  Service-client setup is now flagged as Business-tier.
- Corrected the Container Architecture section of the security doc:
  production runs on Azure Container Apps, not Coolify (Coolify is used
  for a separate application). Fixed in both the Starlight page and the
  legacy file.

#### Changed

- Docker tagging strategy pinned to `ghcr.io/wyre-technology/conduit` (distinct from upstream image path)
- Release workflow hardened: semver + `latest` (main only) + `sha-<short>` tags via `docker/metadata-action`

### Inherited from upstream

- Synced from `wyre-technology/mcp-gateway@main` — see upstream CHANGELOG for itemized
  entries. Track the exact commit range in each release's notes
  (format: `mcp-gateway@<base>..<head>`).

## [Pre-fork baseline]

Entries below predate the Conduit/upstream split and describe the initial
extraction from mcp-gateway plus early Conduit customization work.

### Added

- Lubing USA customer brand with path-based routing (`/lubing` landing, `/lubing/login`)
- Customer brand registry (`src/brand/customers.ts`) for multi-tenant white-label deployments
- Dynamic Google Fonts loading based on per-brand font configuration
- Landing page and login page now accept `BrandConfig` override for customer branding
- Branded landing page with WYRE brand kit (Oswald/Nunito Sans fonts, #EDE947/#00C9DB palette)
- Login chooser page with provider-specific sign-in buttons (Microsoft or Auth0)
- Extended `BrandConfig` with `accentColor`, `headingFont`, `bodyFont`, `borderRadius`
- Updated brand defaults to WYRE official colors and logo URL
- Azure AD multi-tenant OIDC authentication plugin (`src/auth/azure-ad.ts`)
- Auth provider switcher — select Auth0 or Azure AD via `AUTH_PROVIDER` env var
- Admin consent endpoint for customer tenant onboarding (`/auth/admin-consent`)
- `customer_tenants` table and migration for tracking onboarded Azure AD tenants
- Initial extraction from `mcp-gateway` — all existing functionality preserved
- Brand configuration system (`BrandConfig`) for white-label deployments
- Prompt/argument capture in audit logs
- Configurable plan catalog (replaces hardcoded free/pro)
- Usage and value dashboards
- Feature flags and config consolidation
- Comprehensive platform documentation
