{
  "_comment": "Conduit release config. Tag namespace: plain semver (v1.2.3) scoped to this repo — GHCR images are published under ghcr.io/wyre-technology/conduit so upstream mcp-gateway tags never collide. If we ever publish both images under a shared namespace, switch tagFormat to 'conduit-v${version}'.",
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    ["@semantic-release/github", {
      "successComment": false,
      "failComment": false
    }],
    ["@semantic-release/git", {
      "assets": ["CHANGELOG.md", "package.json", "package-lock.json"],
      "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }]
  ]
}
